#!/usr/bin/env bash

join() {
  local IFS="$1"; shift; echo "$*";
}

PLATFORM_ROOT="$(dirname "$(dirname "$(readlink -fm "$0")")")"

SERVICES=(contentservice api-gateway userservice)

docker-compose -f "$PLATFORM_ROOT"/docker-compose.yml logs -f $(join ' ' "${SERVICES[@]}")
